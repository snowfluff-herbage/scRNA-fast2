# ==============================================================================
# 大亚群注释参数
# ==============================================================================
# 本文件使用原始脚本的“cluster编号向量”注释方式，
# 不需要制作并读取人工注释表。
#
# 请先查看result目录中的：
#   1. cluster_top10_marker热图.pdf
#   2. 所有cluster_marker.csv
#   3. 大亚群经典marker_DotPlot.pdf
# 然后在celltype_cluster_map中填写“亚群名称 = cluster编号”。
#
# 名称写在引号中，可以使用中文、英文、空格、斜杠等字符。
# 用户可以自由修改已有名称，也可以新增或删除任意亚群。
# 脚本会自动读取列表中的全部亚群，不需要同步修改分析脚本。
#
# 一个cluster不能同时填写到两个大亚群中。暂不确定的cluster可以不填写，
# 最终会保留为unknown，避免脚本擅自作出生物学注释。
#
# 填写示例（仅作为格式演示，不要直接照搬编号）：
# celltype_cluster_map <- list(
#   "T细胞和NK细胞" = c(0, 3, 7),
#   "B细胞" = c(2, 8),
#   "髓系细胞" = c(1, 5),
#   "新增的大亚群名称" = c(4)
# )

# 下面只是常用大亚群的初始模板。
# 可以直接改变引号中的名称、增加新行或删除不需要的行。
# 每一行末尾除最后一行外都需要保留英文逗号。
celltype_cluster_map <- list(
  "T/NK" = c(),
  "Plasma" = c(),
  "B cell" = c(),
  "Myeloid" = c(),
  "Fibroblast" = c(),
  "Endothelial" = c(),
  "Mast" = c(),
  "Epithelial" = c()
)
